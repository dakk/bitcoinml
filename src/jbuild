(jbuild_version 1)

(library
 ((name        bitcoinml)
  (public_name bitcoinml)
  (libraries (bitstring 
              stdint 
              cryptokit 
              bignum))
  (preprocess  (pps (bitstring.ppx)))
))

