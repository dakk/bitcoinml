(jbuild_version 1)

(library
 ((name        bitcoinml)
  (public_name bitcoinml)
  (libraries (sexplib bitstring stdint cryptokit bignum hex secp256k1))
  (preprocess (pps (ppx_bitstring ppx_sexp_conv)))))

