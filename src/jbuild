(jbuild_version 1)

(library
 ((name        bitcoinml)
  (public_name bitcoinml)
  (libraries (core
              ppx_jane
              ppx_sexp_conv
              sexplib 
              bitstring 
              stdint 
              cryptokit 
              bignum 
              secp256k1))
  (preprocess (pps (ppx_jane ppx_bitstring -no-check ppx_driver.runner)))))

